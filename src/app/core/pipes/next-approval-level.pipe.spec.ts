import { NextApprovalLevelPipe } from './next-approval-level.pipe';

describe('NextApprovalLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new NextApprovalLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
