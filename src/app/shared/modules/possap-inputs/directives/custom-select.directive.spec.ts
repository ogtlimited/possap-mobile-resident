import { CustomSelectDirective } from './custom-select.directive';

describe('CustomSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomSelectDirective();
    expect(directive).toBeTruthy();
  });
});
